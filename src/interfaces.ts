export interface ConfigAttributes{
    sender_id: string,
    password: string,
    username: string,
    url: string,
}

export interface TextAttributes{
    environment: string;
    recipient: string;
    message: string;
}
